#the ingest script downloads this fresh every run and deletes it after,
#but just in case a run dies halfway
oracle-cards.json

#python junk
__pycache__/
*.pyc

#editor stuff
.vscode/

#local database connection string, never commit this
.env

#the roadmap is private: it carries the strategy, what the competition can
#and cant do, and what not to build. it stays on disk and out of the repo
TODO.md

#in case a virtualenv ever lives in here
venv/
.venv/

#finetune outputs, all reproducible: the scripts remake the jsonls and csv,
#the model itself lives on hugging face. train_rewordings.jsonl is NOT in
#this list on purpose, its hand written and cant be regenerated
finetune/mtg-tuned-*/
finetune/train_pairs.jsonl
finetune/train_negatives.jsonl
finetune/train_triplets.jsonl
finetune/train_keywords.jsonl
finetune/bakeoff_results.csv
finetune/sanity_report.txt
